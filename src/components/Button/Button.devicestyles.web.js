import { StyleSheet } from 'react-native'
// import { theme } from '../../styles/theme'

export default StyleSheet.create({
  circle: {
    borderRadius: '50%'
  },
  disabled: {
    cursor: 'not-allowed'
  },
  loadingOverlay: {
    cursor: 'progress'
  },
  root: {
    cursor: 'pointer'
  }
})
